services:
  # --- Infrastructure ---
  redis:
    image: redis:7-alpine
    container_name: ai-training-redis-06
    ports:
      - "6379:6379"
    volumes:
      - build06_redis_data:/data
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  postgres:    
    image: pgvector/pgvector:pg15
    container_name: ai-training-DB-06
    restart: always
    environment:
      POSTGRES_USER: Postgres
      POSTGRES_PASSWORD: Postgres
      POSTGRES_DB: chatdb
    ports:   
      - "5433:5432"
    volumes:
      - build06_postgres_data:/var/lib/postgresql/data  

volumes:
  build06_redis_data:      
  build06_postgres_data:
